# knowledge.status.md

*Rewritten freely (at minimum at session end); owes nothing to its previous version.*

**Where things stand:** the working node is **newborn** (entry 0001) — its four docs and
the shipped knowledge branches in place, copied whole from the workbench's knowledge seed.
The project is deliberately undefined; the problem plane is empty — its artifacts and
record are earned when problem work starts. Position per the realization skeleton
(`realization/workbench-backend-realization.map.md`): **`initialization`**, step 1 (node
bootstrapped) done, standing before **step 2 — project defined**.

**Shape** *(snapshot; the repo is the master)*:

```text
repo root
└── internals/
    │                                    ← the problem plane: problem artifacts + the
    │                                      problem's log.md / status.md — all earned by
    │                                      need, none yet
    └── knowledge/                       ← the working node, shipped whole
        ├── knowledge.vision.md
        ├── knowledge.log.md             entry 0001
        ├── knowledge.status.md
        ├── knowledge-steward.instructions.md
        │                                (fork.log.md — born at the first deviation)
        ├── core/                        general node-system knowledge
        │   ├── workbench-nodes.model.md
        │   ├── workbench-roles.model.md
        │   ├── workbench-git.convention.md
        │   └── workbench-naming.convention.md
        └── realization/                 the backend-shaped set
            ├── workbench-five-layer-system.model.md
            ├── workbench-correctness-driven-backend.model.md
            ├── workbench-backend-realization.map.md
            └── workbench-doc-projection.skeleton.md
```

**Unsettled:**
- The **project definition** — arrives as ordinary input or is worked in session;
  `definition` kind born then; the problem plane's record born with the first problem
  decisions.
- Everything downstream of it (map instantiation, working ground, first slice) — per the
  skeleton, at its steps.
- **Doc projection** — the shipped skeleton
  (`realization/workbench-doc-projection.skeleton.md`, unlived) is instantiated as this
  project's own projection rules when the work reaches them; cadence is this project's
  decision, stated in its vision.

**Next:** define the project.
