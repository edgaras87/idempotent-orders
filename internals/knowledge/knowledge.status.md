# knowledge.status.md

*Rewritten freely (at minimum at session end); owes nothing to its previous version.*

**Where things stand:** the node runs; the fork carries six deviations (all unlived,
owed confirmation at finalization — `fork.log.md` 0001–0006): five in the realization
map, and the sixth in the **doc-projection skeleton** — projection anchored at *problem
framed*'s exit (the thin README: intent's why, the L2 what, the planned-invariants
list, the method note, a status line), the former first anchor renamed *system
bootstrapped* and trimmed to its delta (stack, run-notes), the intent row added to the
what-projects table. The node has also made its **second decision**
(`knowledge.log.md` 0002): the project adopts the skeleton's exit-anchored cadence
from the *problem framed* anchor on — projection owed at the map's named exits,
event-driven; skipping one is a logged decision. Stated in the vision's project
section. Preparation reads: node bootstrapped → problem framed → infrastructure
established → system bootstrapped; *problem framed*'s full exit picture now includes
the thin README owed. The project remains deliberately undefined; the problem plane is
empty — its artifacts and record earned when problem work starts.

Position, in the map's names: **`preparation`** — *node bootstrapped* done, standing
before **`problem framed`**, at its first inner step, **project-intent** (unlived;
worked in session by the steward per the standing lean — thinker's birth deferred to
lived evidence, `system-definition` its natural earliest trigger; final call made when
the step starts).

**Shape** *(snapshot; the repo is the master)*:

```text
repo root
└── internals/
    │                                    ← the problem plane: problem artifacts + the
    │                                      problem's log.md / status.md — all earned by
    │                                      need, none yet
    └── knowledge/                       ← the working node
        ├── knowledge.vision.md          cadence setting added; names synced
        ├── knowledge.log.md             entries 0001–0002
        ├── knowledge.status.md
        ├── knowledge-steward.instructions.md
        ├── fork.log.md                  entries 0001–0006
        ├── core/                        general node-system knowledge
        │   ├── workbench-nodes.model.md
        │   ├── workbench-roles.model.md
        │   ├── workbench-git.convention.md
        │   └── workbench-naming.convention.md
        └── realization/                 the backend-shaped set
            ├── workbench-five-layer-system.model.md
            ├── workbench-correctness-driven-backend.model.md
            ├── workbench-backend-realization.map.md       ← amended (fork.log 0001–0005)
            └── workbench-doc-projection.skeleton.md       ← amended (fork.log 0006)
```

**Unsettled:**
- **Project-intent** — work it, take it as input, or trim it: the first decision of
  problem framed; its first act births the problem plane's record. Lean: worked in
  session by the steward.
- The **definition** and the **slice-registry's birth** — the chain's remaining inner
  steps; `definition` kind and the registry's kind born at their first instances; the
  definition step is the earliest natural trigger for the thinker-role question, judged
  on lived evidence from intent.
- **Steward lens refresh** — `knowledge-steward.instructions.md` still summarizes the
  old map picture (old phase/step names, pre-0004 step sequence, no cadence line);
  non-authoritative (the vision wins on conflict), so not urgent — refresh when the
  lens is next touched.
- **Doc projection's first run** — owed at *problem framed*'s exit per the adopted
  cadence; the thin README its first artifact.

**Next:** work project-intent (its first act births the problem plane's record), then
the chain — definition, registry — and the thin README at the step's exit.