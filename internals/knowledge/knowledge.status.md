# knowledge.status.md

*Rewritten freely (at minimum at session end); owes nothing to its previous version.*

**Where things stand:** the node runs; the fork has its first deviation. The realization
skeleton (`realization/workbench-backend-realization.map.md`) was reshaped in place —
the problem's reasoning chain grouped as the composite step **prepare-thinking-part**
(inner: project-intent → system-definition → slice-registry born), and scheduled map
instantiation replaced by **need-driven adoption**: the project works against the fork's
map directly; execution state lives in the project's record, coupled by names only.
`fork.log.md` born with entry 0001 (the deviation's why); the vision corrected to match.
The project remains deliberately undefined; the problem plane is empty — its artifacts
and record earned when problem work starts.

Position, in the map's names: **`initialization`** — *node bootstrapped* done, standing
before **`prepare-thinking-part`**, at its first inner step, **project-intent** (unlived;
may be worked in session, arrive as ordinary input, or be deliberately trimmed — a
decision not yet made).

**Shape** *(snapshot; the repo is the master)*:

```text
repo root
└── internals/
    │                                    ← the problem plane: problem artifacts + the
    │                                      problem's log.md / status.md — all earned by
    │                                      need, none yet
    └── knowledge/                       ← the working node
        ├── knowledge.vision.md
        ├── knowledge.log.md             entry 0001
        ├── knowledge.status.md
        ├── knowledge-steward.instructions.md
        ├── fork.log.md                  entry 0001 — the skeleton reshape
        ├── core/                        general node-system knowledge
        │   ├── workbench-nodes.model.md
        │   ├── workbench-roles.model.md
        │   ├── workbench-git.convention.md
        │   └── workbench-naming.convention.md
        └── realization/                 the backend-shaped set
            ├── workbench-five-layer-system.model.md
            ├── workbench-correctness-driven-backend.model.md
            ├── workbench-backend-realization.map.md   ← amended (fork.log 0001)
            └── workbench-doc-projection.skeleton.md
```

**Unsettled:**
- **Project-intent** — work it, take it as input, or trim it: the first decision of
  prepare-thinking-part.
- The **definition** and the **slice-registry's birth** — the chain's remaining inner
  steps; `definition` kind and the registry's kind born at their first instances; the
  problem plane's record born with the first problem decisions.
- **Steward lens refresh** — `knowledge-steward.instructions.md` still summarizes the
  old map picture (instantiation, old step names); non-authoritative (the vision wins on
  conflict), so not urgent — refresh when the lens is next touched.
- **Doc projection** — the shipped skeleton (unlived) instantiated when the work reaches
  it; cadence this project's decision, stated in the vision.

**Next:** decide how project-intent is handled, then work the chain.
