# status.md

*The problem's snapshot. Rewritten freely (at minimum at session end); owes nothing to
its previous version. Subject: the problem only — the node's own standing lives in
`knowledge/knowledge.status.md`.*

**Where things stand:** the reasoning chain **stands complete** — intent (define),
definition (capture, flip pending this plan's next step), and now the registry
(`slices.registry.md`, the birth's why: this log's 0003): four slices from the
definition's five concerns — S1 duplicate suppression **(chosen-next)**, S2 the
concurrent-duplicate race, S3 safe response replay, S4 crash during creation — with
concern 4 (request identity) folded into S1's spec work as the contract the invariants
are defined against; working order S1 → S2 → S3 → S4, re-decided at each close.

Position, in the map's names: **`preparation` → `problem framed`, at its exit** — all
three inner steps done; the exit's owed projection (the thin README) and the
definition's genre flip are the remaining acts of the current change plan, then the
phase-step is left behind and the next is **infrastructure established** (evaluated
from this chain's outputs: the definition's environment reasoning, the registry's
needs).

**Shape** *(snapshot; the repo is the master)*:

```text
internals/
├── project.intent.md        the why — define
├── system.definition.md     the what — capture (flip in this plan's step 4)
├── slices.registry.md       the control point — S1 chosen-next
├── log.md                   entries 0001–0003
├── status.md
└── knowledge/               the working node (its own status inside)
```

**Unsettled:**
- **The definition's genre flip** — the registry populated cleanly from the concern
  map; the flip to define is this plan's next step.
- **The thin README** — the exit's owed projection: intent's why, the L2 what, the
  planned-invariants list (the registry's four lines), the method note, a status line.
- **Infrastructure established** — the next phase-step after the exit: environment and
  services evaluated from the chain's outputs, constrained by need.
- **A discovery register** — parked idea, not yet earned: where mid-execution
  discoveries land (slice-shaped ones are definition revisions + registry rows via
  existing mechanisms; an unshaped one would earn a small registry section at its
  first instance).

**Next:** the definition's flip, then the README — closing *problem framed*.