# status.md

*The problem's snapshot. Rewritten freely (at minimum at session end); owes nothing to
its previous version. Subject: the problem only — the node's own standing lives in
`knowledge/knowledge.status.md`.*

**Where things stand:** the problem plane is born. The project is **idempotent order
creation** — its intent landed as define (`project.intent.md`): the service as sole
authority on purchase-intent-to-order, once; the method grounded on it; the surface
written for a reviewer judging guarantee depth in minutes; cross-service and time-based
behaviour out of scope; done checkable (slices closed on evidence, guarantees legible,
walkthrough filled and flowed back, node closed clean). The system itself remains
undefined — the definition is the next work.

Position, in the map's names: **`preparation` → `problem framed`** — *project-intent*
done; standing before **system-definition** (the definition drawn with the five-layer
model, `definition` kind born at its landing), then *slice-registry born*. The thin
README is owed at the phase-step's exit, per the adopted projection cadence.

**Shape** *(snapshot; the repo is the master)*:

```text
internals/
├── project.intent.md        the why — define
├── log.md                   entry 0001
├── status.md
└── knowledge/               the working node (its own status inside)
```

**Unsettled:**
- **The definition** — the next inner step: L1–L4 drawn with the five-layer model;
  earliest natural trigger for the node's thinker-role question (judged there, on lived
  evidence from intent — the node's call, in its log).
- **The slice-registry's birth** — after the definition: its concern map made
  actionable, first slice chosen-next; the registry's kind born at that instance.
- **The thin README** — owed at *problem framed*'s exit: intent's why (one paragraph),
  the L2 what, the planned-invariants list, the method note, a status line.

**Next:** the definition.